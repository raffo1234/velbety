---
import TabNavigationImages from "../TabNavigationImages";
import areas from "../../data/areas";

const images = areas.map((area) => area.images);
const hrefs = areas.map((area) => area.href);
const title = "Lo Que Hemos Creado";
const description = "Sitios web diseñados para destacar y crecer.";
---

<TabNavigationImages
  client:only
  title={title}
  description={description}
  images={images}
  hrefs={hrefs}
/>
